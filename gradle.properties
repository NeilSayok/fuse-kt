kotlin.code.style=official

# Publishing configuration
# Replace these values with your actual credentials
# You can also set these as environment variables or in ~/.gradle/gradle.properties

# Sonatype OSSRH credentials (get from https://issues.sonatype.org)
#ossrhUsername=your_sonatype_username
#ossrhPassword=your_sonatype_password

# PGP signing key (base64 encoded private key)
#signingKey=your_base64_encoded_private_key
#signingPassword=your_private_key_password

# Project metadata
GROUP=io.github.yourusername
VERSION_NAME=1.0.0
